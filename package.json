{
  "name": "deploy",
  "version": "1.0.0",
  "description": "git",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "surmon",
  "license": "ISC",
  "dependencies": {
    "github-webhook-handler": "^0.6.0",
    "shelljs": "^0.7.5"
  }
}
